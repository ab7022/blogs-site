<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%- include("includes/head",{title:"Post title"}) %>
    <link rel="stylesheet" href="/styles/styles.css">
</head>
<body>
    <%- include("includes/header") %>
    <main id="all-detail">
        <h1>Update A Post</h1>
        <form action="/posts/<%= post._id %>/edit" method="POST">

            <div class="form-control">
                <label for="title">Title</label>
                <input type="text" name="title" id="title" required value="<%= post.title %>">
            </div>
            <div class="form-control">
                <label for="summary">Summary</label>
                <input type="text" name="summary" id="summary" value="<%= post.summary %>" >
            </div>
            <div class="form-control">
                <label for="content">Content</label>
                <textarea name="content" id="content" rows="5" required><%= post.body %></textarea>            
            </div>

            <button class="btn">Update Post</button>
</section>
        </form>
        
    </main>
</body>
</html>